# RAG-based-ReAct-Agent

## Demo

Run a small, self-contained demo that exercises the ReAct loop with a fake LLM client:

```bash
python demo.py
```

The demo prints the final answer generated by the ReAct runtime.
